// Pre-render all pages into static HTML at build time (Static Site Generation).
// This route is fully client-interactive but has no server-side logic,
// so it prerenders cleanly and hydrates in the browser.
export const prerender = true;